{
  "name": "Script Execution Example Workflow",
  "description": "Demonstrates custom Python script execution in workflows",
  "variables": {
    "input_value": 10
  },
  "steps": [
    {
      "id": 1,
      "type": "set_variable",
      "name": "input_value",
      "value": 10,
      "value_type": "int",
      "pre_delay": 0,
      "comment": "Set initial input value"
    },
    {
      "id": 2,
      "type": "script",
      "code": "# Calculate factorial\ndef factorial(n):\n    if n <= 1:\n        return 1\n    return n * factorial(n - 1)\n\nresult = factorial(input_value)\nprint(f'Factorial of {input_value} is {result}')",
      "output_var": "factorial_result",
      "pre_delay": 0.1,
      "comment": "Calculate factorial using script"
    },
    {
      "id": 3,
      "type": "script",
      "code": "# Simple string manipulation\ntext = 'Hello World'\nupper_text = text.upper()\nreversed_text = text[::-1]\nprint(f'Original: {text}')\nprint(f'Upper: {upper_text}')\nprint(f'Reversed: {reversed_text}')",
      "output_var": "string_result",
      "pre_delay": 0.1,
      "comment": "String manipulation example"
    },
    {
      "id": 4,
      "type": "delay",
      "seconds": 0.5,
      "pre_delay": 0.1,
      "comment": "End of script workflow"
    }
  ]
}
